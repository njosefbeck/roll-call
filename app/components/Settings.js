var React = require('react');

var Settings = React.createClass({
    render: function() {
        return (
            <div>
                Settings page
            </div>
        )
    }
});

module.exports = Settings;